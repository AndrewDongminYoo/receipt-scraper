# Day 5 Polish Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to
> implement this plan task-by-task.

**Goal:** Finalize the prototype flow with consistent UI states, light shared UI
extraction, updated documentation, and a recorded follow-up plan for production
receipt capture.

**Architecture:** Keep the existing screen-led structure. Extract only a small
presentation layer under `src/components`, add the missing reward fetch error
state, and document the production camera/OCR pipeline as a future extension
instead of building it now.

**Tech Stack:** React Native CLI, TypeScript, React Navigation, TanStack Query,
React Hook Form, Jest, Testing Library

---

## Task 1: Lock the missing reward error behavior with a failing test

### **Files:**

- Create: `__tests__/RewardResultScreen.test.tsx`
- Modify: `src/screens/RewardResultScreen.tsx`

### **Steps:**

1. Write a test that makes `fetchLatestRewardResult` fail once, expects an error
   card, presses retry, and expects the empty reward state.
2. Run `yarn test __tests__/RewardResultScreen.test.tsx --runInBand` and verify
   it fails for the current implementation.
3. Update `RewardResultScreen.tsx` to render explicit error UI and retry.
4. Re-run the same test until it passes.

## Task 2: Extract the shared Day 5 UI primitives

### **Files:**

- Create: `src/components/ScreenHeader.tsx`
- Create: `src/components/SectionCard.tsx`
- Create: `src/components/StateCard.tsx`
- Modify: `src/screens/ReceiptUploadScreen.tsx`
- Modify: `src/screens/ReceiptListScreen.tsx`
- Modify: `src/screens/SurveyScreen.tsx`
- Modify: `src/screens/RewardResultScreen.tsx`

### **Steps:**

1. Extract the shared title/description block into `ScreenHeader`.
2. Extract the bordered white card container into `SectionCard`.
3. Extract reusable loading/empty/error/info presentation into `StateCard`.
4. Refactor the four feature screens to use the new primitives without changing
   the flow order or adding new features.
5. Re-run the screen tests most affected by the refactor.

## Task 3: Update prototype and follow-up documentation

### **Files:**

- Modify: `README.md`
- Create: `docs/plans/2026-04-03-day5-polish-design.md`
- Create: `docs/plans/2026-04-03-day5-polish.md`

### **Steps:**

1. Document the prototype boundary around image-picker uploads.
2. Document the intentional technical debt and future expansion points.
3. Record the deferred production receipt capture pipeline with the approved
   OCR, payload, feedback, and Firebase Remote Config decisions.

## Task 4: Run the final Day 5 verification pass

### **Files:**

- Verify only

### **Steps:**

1. Run `yarn test`.
2. Run `yarn lint`.
3. Report the exact verification results and any residual manual risks.
